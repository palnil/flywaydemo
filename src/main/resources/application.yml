spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/CRM
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: false
    generate-ddl: true
    hibernate.ddl-auto: validate

logging:
  level:
    org:
      hibernate:
        type: trace

